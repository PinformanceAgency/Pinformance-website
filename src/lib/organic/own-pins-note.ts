/** Split from own-pins.ts so client components can import the sentence
 *  without pulling the Pinterest client into the browser bundle. */
export const OWN_PINS_FILTER_NOTE =
  "Organic, your pins, image + video pins only. Product (catalogue) pins are left out: they run on paid ads but Pinterest counts them as organic.";
